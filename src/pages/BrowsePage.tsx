import { useMemo, useState } from 'react'
import { ChevronRight, Compass, MessageCircle, Search, SlidersHorizontal, Star } from 'lucide-react'
import { gigs, categories } from '../data/gigs'
import { GigCard } from '../components/GigCard'
import { Step } from '../components/Step'
import type { Category, Gig } from '../types'

type BrowsePageProps = { onOpenGig: (gig: Gig) => void }

export function BrowsePage({ onOpenGig }: BrowsePageProps) {
  const [activeCategory, setActiveCategory] = useState<'All' | Category>('All')
  const [query, setQuery] = useState('')
  const filteredGigs = useMemo(() => gigs.filter((gig) => (activeCategory === 'All' || gig.category === activeCategory) && `${gig.title} ${gig.person} ${gig.category}`.toLowerCase().includes(query.toLowerCase())), [activeCategory, query])

  return <main id="browse" className="mx-auto max-w-[1440px] px-5 pb-24 pt-10 lg:px-8 lg:pt-14"><div className="mb-10 flex flex-col justify-between gap-6 md:flex-row md:items-end"><div className="max-w-xl"><p className="mb-3 font-mono text-[11px] uppercase tracking-[.18em] text-sun">01 · The board</p><h1 className="font-display text-4xl font-bold leading-[1.05] tracking-tight sm:text-5xl">Find someone who<br /><span className="text-coral">gets it done.</span></h1><p className="mt-4 max-w-md text-sm leading-6 text-white/55">Useful things, made by people around you. Browse the board or pin your own skill for the community to find.</p></div><div className="flex items-center gap-2 text-xs text-white/45"><span className="h-2 w-2 rounded-full bg-mint" /> 128 active gigs this week <ChevronRight size={14} /></div></div><section className="rounded-2xl border border-white/10 bg-board-alt/80 p-4 shadow-note sm:p-5"><div className="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><label className="flex min-h-12 flex-1 items-center gap-3 rounded-xl border border-white/10 bg-board px-4 text-white/45 focus-within:border-coral/60"><Search size={18} /><input value={query} onChange={(event) => setQuery(event.target.value)} className="w-full bg-transparent text-sm text-paper outline-none placeholder:text-white/35" placeholder="Search gigs, skills, or people..." /></label><button className="flex items-center justify-center gap-2 rounded-xl border border-white/15 px-4 py-3 text-sm text-white/65 hover:border-white/40"><SlidersHorizontal size={16} /> Filters</button></div><div className="mt-4 flex gap-2 overflow-x-auto pb-1">{categories.map((category) => <button key={category} onClick={() => setActiveCategory(category)} className={`whitespace-nowrap rounded-full border px-4 py-2 font-mono text-[11px] transition ${activeCategory === category ? 'border-coral bg-coral font-medium text-board' : 'border-white/15 text-white/60 hover:border-white/40'}`}>{category}</button>)}</div></section><div className="mb-4 mt-10 flex items-center justify-between"><div><p className="font-display text-xl font-bold">Pinned this week</p><p className="mt-1 text-xs text-white/40">{filteredGigs.length} opportunities on the board</p></div><button className="hidden items-center gap-2 text-xs font-semibold text-coral sm:flex">Sort: Recommended <ChevronRight size={14} /></button></div>{filteredGigs.length ? <div className="grid grid-cols-1 gap-x-6 gap-y-8 sm:grid-cols-2 xl:grid-cols-3">{filteredGigs.map((gig, index) => <GigCard key={gig.id} gig={gig} index={index} onOpen={() => onOpenGig(gig)} />)}</div> : <div className="rounded-xl border border-dashed border-white/20 py-16 text-center text-sm text-white/50">No gigs match that search yet.</div>}<section id="how" className="mt-28 grid gap-6 border-t border-white/10 pt-10 md:grid-cols-3"><Step icon={<Compass size={18} />} number="01" title="Find your person" text="Search the board for a useful skill, a fair price, and a good fit." /><Step icon={<MessageCircle size={18} />} number="02" title="Start a conversation" text="Request a gig and work out the details directly with the provider." /><Step icon={<Star size={18} />} number="03" title="Build the board" text="When the work is done, leave a rating and help the next person choose." /></section></main>
}
